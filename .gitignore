.flavor
FLAVOR.md
.superharness
node_modules

# Python
__pycache__/
*.pyc
*.egg-info/
*.egg
auth-server/keys/
auth-server/auth.db
*.db

# Env files (API keys, secrets)
.env

# Deployment scripts that embed server credentials
setup_ssh.py

# IDE
.idea/
.vscode/

# Build
frontend/dist/
auth-server/.pytest_cache/
api-gateway/.pytest_cache/
# Transient smoke-test log held open while the dev gateway runs
api-gateway/smoke_gw.log
.pytest_cache/
# superharness ralph runtime state (per-task tracking + retry)
.claude/superharness/ralph/
# superharness brainstorm mind-map session state (transient)
.claude/superharness/brainstorm/
# superharness brainstorm mind-map session state (transient)
.superharness/

.worktrees/
